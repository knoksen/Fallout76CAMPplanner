# Changelog v4

- Added **layer lock** controls per layer with visual lock overlay on canvas items
- Added **approximate surface CAMP radius** overlay for surface-mode planning
- Added **approximate turret coverage arcs** based on turret rotation
- Locked layers are now respected in click-selection, marquee-selection, erase and placement
- Item list now labels locked-layer items
- Added `sample-defense-layout.json` example project
- Fixed duplicate west-edge anchor candidate in wall/door snapping
